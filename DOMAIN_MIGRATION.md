# 🌐 Перенос домена www.playandjump.de на другой Python Anywhere

## 📋 План действий:

1. **На ТВОЕМ аккаунте:** Отключить домен (но не удалять)
2. **На КЛИЕНТСКОМ аккаунте:** Настроить веб-приложение
3. **На КЛИЕНТСКОМ аккаунте:** Добавить домен
4. **У регистратора домена:** Обновить DNS записи
5. **Проверка:** Убедиться, что домен работает

---

## 🔴 ШАГ 1: На ТВОЕМ аккаунте (отключение домена)

### Вариант 1: Просто отключить (рекомендуется)

**НЕ удаляй веб-приложение!** Просто:

1. Зайди в раздел **Web** на Python Anywhere
2. Нажми на веб-приложение `www.playandjump.de`
3. **НЕ нажимай "Delete"** - просто оставь как есть
4. После настройки на новом аккаунте, домен автоматически переключится

**Или можно временно переименовать:**
- В разделе **Web** нажми **Edit** рядом с доменом
- Временно измени домен на что-то вроде `old-playandjump.pythonanywhere.com`
- Это освободит `www.playandjump.de` для нового аккаунта

### Вариант 2: Удалить веб-приложение (если точно не нужен)

**⚠️ ВНИМАНИЕ:** Это удалит веб-приложение полностью. Используй только если уверен!

1. Зайди в раздел **Web**
2. Нажми на веб-приложение `www.playandjump.de`
3. Прокрути вниз и найди кнопку **"Delete"** (красная)
4. Подтверди удаление

**НО:** Это не обязательно! Можно просто оставить и настроить новый.

---

## 🟢 ШАГ 2: На КЛИЕНТСКОМ аккаунте (настройка веб-приложения)

### ШАГ 2.1: Создание веб-приложения

1. Зайди в раздел **Web** на клиентском Python Anywhere
2. Нажми **"+ Add a new web app"**
3. Выбери **Manual configuration**
4. Выбери **Python 3.10**
5. Нажми **Next**

### ШАГ 2.2: Настройка кода

В разделе **Code** укажи:

- **Source code:** `/home/имя_клиента/playandjump`
- **Working directory:** `/home/имя_клиента/playandjump`

### ШАГ 2.3: Настройка виртуального окружения

В разделе **Virtualenv** укажи:

- **Virtualenv:** `/home/имя_клиента/playandjump/venv`

(Если venv еще не создан, создай его сначала)

### ШАГ 2.4: Редактирование WSGI файла

1. В разделе **Code** найди **"WSGI configuration file"**
2. Нажми на **карандаш** (иконка редактирования)
3. Замени содержимое на:

```python
import os
import sys

# add your project directory to the sys.path
project_home = '/home/имя_клиента/playandjump'
if project_home not in sys.path:
    sys.path.insert(0, project_home)

# set environment variable to tell django where your settings.py is
os.environ['DJANGO_SETTINGS_MODULE'] = 'playandjump.settings_pythonanywhere'

# serve django via WSGI
from django.core.wsgi import get_wsgi_application
application = get_wsgi_application()
```

**ВАЖНО:** Замени `имя_клиента` на реальное имя пользователя клиента!

4. Нажми **Save**

---

## 🌐 ШАГ 3: Добавление домена www.playandjump.de

### Вариант 1: Через веб-интерфейс

1. В разделе **Web** → **Domains**
2. Нажми **"+ Add a new domain"**
3. Введи: `www.playandjump.de`
4. Нажми **Add**
5. **ВАЖНО:** Также добавь `playandjump.de` (без www)

### Вариант 2: При создании веб-приложения

Если веб-приложение еще не создано, можно указать домен сразу при создании.

---

## 🔧 ШАГ 4: Обновление DNS записей у регистратора домена

### ШАГ 4.1: Узнай CNAME на новом аккаунте

После добавления домена на клиентском Python Anywhere:

1. Зайди в раздел **Web** → **Domains**
2. Найди домен `www.playandjump.de`
3. Там будет указан **CNAME**, например: `webapp-XXXXXX.pythonanywhere.com`
4. **Запиши этот CNAME!** Он понадобится для DNS

### ШАГ 4.2: Обновление DNS у регистратора

Зайди в панель управления доменом у регистратора (например, Namecheap, GoDaddy, и т.д.):

#### Для www.playandjump.de:

1. Найди DNS записи для домена
2. Найди CNAME запись для `www`
3. Измени значение на новый CNAME: `webapp-XXXXXX.pythonanywhere.com`
4. Сохрани изменения

#### Для playandjump.de (без www):

1. Найди A-запись для `@` (корневой домен)
2. Узнай IP адрес Python Anywhere (см. ниже)
3. Или создай A-запись: `@` → IP адрес Python Anywhere

**Как узнать IP адрес Python Anywhere:**
- В разделе **Web** → **Domains** обычно указан IP
- Или напиши в поддержку Python Anywhere

**Альтернатива:** Можно использовать CNAME для корневого домена (если регистратор поддерживает):
- `@` → `webapp-XXXXXX.pythonanywhere.com`

### ШАГ 4.3: Время распространения DNS

После обновления DNS записей:
- **Обычно:** 5-30 минут
- **Максимум:** до 48 часов (но обычно быстрее)

Проверить можно командой:
```bash
nslookup www.playandjump.de
```

Или через онлайн сервис: https://dnschecker.org/

---

## ✅ ШАГ 5: Настройка статических файлов

В разделе **Web** → **Static files** добавь:

- **URL:** `/static/` → **Directory:** `/home/имя_клиента/playandjump/staticfiles`
- **URL:** `/media/` → **Directory:** `/home/имя_клиента/playandjump/media`

---

## 🔄 ШАГ 6: Перезагрузка веб-приложения

1. В разделе **Web** нажми зеленую кнопку **"Reload www.playandjump.de"**
2. Дождись завершения перезагрузки (обычно 10-30 секунд)

---

## ✅ ШАГ 7: Проверка

### Проверка 1: Доступность сайта

1. Открой в браузере: `https://www.playandjump.de`
2. Сайт должен открыться

### Проверка 2: Админка

1. Открой: `https://www.playandjump.de/admin/`
2. Войди в админку
3. Проверь, что все данные на месте

### Проверка 3: Проверка DNS

```bash
# В консоли (любой)
nslookup www.playandjump.de
```

Должен показать правильный CNAME.

---

## ⚠️ Важные моменты:

### 1. Два домена
- `www.playandjump.de` → CNAME
- `playandjump.de` (без www) → A-запись или CNAME

### 2. SSL сертификат
- Python Anywhere автоматически выдает SSL сертификат
- После настройки DNS подожди 5-10 минут
- Сертификат выдастся автоматически

### 3. Время простоя
- Между отключением старого и подключением нового может быть простоя 5-30 минут
- Это нормально из-за DNS распространения

### 4. Если что-то не работает

**DNS не обновился:**
- Подожди еще 30 минут
- Очисти кэш DNS на компьютере: `ipconfig /flushdns` (Windows)

**Сайт не открывается:**
- Проверь, что веб-приложение перезагружено
- Проверь логи в разделе **Web** → **Error log**
- Проверь, что DNS записи правильные

**SSL сертификат не работает:**
- Подожди 10-15 минут после настройки DNS
- Python Anywhere автоматически выдает сертификат

---

## 📝 Чек-лист переноса:

- [ ] На старом аккаунте: веб-приложение отключено или переименовано
- [ ] На новом аккаунте: веб-приложение создано
- [ ] WSGI файл настроен правильно
- [ ] Виртуальное окружение настроено
- [ ] Домен `www.playandjump.de` добавлен
- [ ] Домен `playandjump.de` добавлен (без www)
- [ ] DNS записи обновлены у регистратора
- [ ] Статические файлы настроены
- [ ] Медиа файлы настроены
- [ ] Веб-приложение перезагружено
- [ ] Сайт открывается по новому адресу
- [ ] Админка работает
- [ ] SSL сертификат работает

---

## 🆘 Если что-то пошло не так:

### Откат (если нужно вернуть на старый аккаунт):

1. На старом аккаунте добавь домен обратно
2. Настрой веб-приложение
3. Обнови DNS записи обратно
4. Перезагрузи

### Поддержка:

Если возникли проблемы, напиши в поддержку Python Anywhere:
- https://www.pythonanywhere.com/contact/
- Или через вкладку "Help" в панели управления


