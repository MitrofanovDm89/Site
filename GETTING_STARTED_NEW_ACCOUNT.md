# 🚀 Начало работы на новом Python Anywhere аккаунте

## 📋 Способ 1: Клонирование из Git (РЕКОМЕНДУЕТСЯ)

### ШАГ 1: Открыть консоль Bash

1. На главной странице (Dashboard) найди раздел **"Recent Consoles"**
2. Нажми кнопку **"$ Bash"** (или зайди в раздел **"Consoles"** и нажми **"Bash"**)
3. Откроется консоль

### ШАГ 2: Клонировать проект из Git

В открывшейся консоли выполни команды:

```bash
# Перейди в домашнюю директорию
cd ~

# Клонируй проект из Git
git clone https://github.com/MitrofanovDm89/Site.git playandjump

# Перейди в папку проекта
cd playandjump

# Проверь, что файлы скопировались
ls -la
```

**Что должно появиться:**
- `catalog/` - папка приложения каталога
- `main/` - папка основного приложения
- `playandjump/` - папка настроек
- `manage.py` - файл управления Django
- `requirements.txt` - файл зависимостей
- и другие файлы проекта

### ШАГ 3: Создать виртуальное окружение

```bash
# Создай виртуальное окружение
python3.10 -m venv venv

# Активируй виртуальное окружение
source venv/bin/activate

# Обнови pip
pip install --upgrade pip

# Установи зависимости
pip install -r requirements_pythonanywhere.txt
```

**Если файла `requirements_pythonanywhere.txt` нет**, установи вручную:
```bash
pip install Django>=4.2,<5.0 django-ckeditor>=6.7.0 Pillow>=10.0.0 whitenoise>=6.5.0
```

### ШАГ 4: Выполнить миграции

```bash
# Убедись, что виртуальное окружение активировано (должно быть (venv) в начале строки)
# Если нет, выполни: source venv/bin/activate

# Выполни миграции базы данных
python manage.py migrate
```

### ШАГ 5: Создать суперпользователя (админку)

```bash
python manage.py createsuperuser
```

Введи:
- Username (логин)
- Email (можно пропустить)
- Password (пароль)
- Password (повтор пароля)

---

## 📁 Способ 2: Загрузка файлов вручную (если Git не работает)

### ШАГ 1: Открыть раздел Files

1. Нажми на вкладку **"Files"** в верхней навигации
2. Откроется файловый менеджер

### ШАГ 2: Создать папку для проекта

1. В разделе **"Directories"** (слева)
2. В поле ввода введи: `playandjump`
3. Нажми **"New directory"**

### ШАГ 3: Загрузить файлы

**Вариант А: Загрузить архив проекта**

1. Если у тебя есть ZIP архив проекта:
   - Нажми **"Upload a file"** в разделе **"Files"**
   - Выбери ZIP файл
   - Дождись загрузки
   - Распакуй архив (через консоль Bash)

2. В консоли Bash:
```bash
cd ~
unzip playandjump.zip  # замени на имя твоего архива
```

**Вариант Б: Загрузить файлы по одному**

1. Создай структуру папок через Files:
   - `playandjump/`
   - `playandjump/catalog/`
   - `playandjump/main/`
   - `playandjump/playandjump/`
   - и т.д.

2. Загружай файлы через **"Upload a file"** в нужные папки

**⚠️ ВНИМАНИЕ:** Этот способ долгий и неудобный. Лучше использовать Git (Способ 1).

---

## 🔍 Проверка после загрузки

### Проверка 1: Файлы на месте

В консоли Bash:
```bash
cd ~/playandjump
ls -la
```

Должны быть видны:
- `manage.py`
- `catalog/`
- `main/`
- `playandjump/`
- и другие папки

### Проверка 2: Зависимости установлены

```bash
source venv/bin/activate
pip list
```

Должны быть видны:
- Django
- django-ckeditor
- Pillow
- whitenoise

### Проверка 3: Миграции выполнены

```bash
source venv/bin/activate
python manage.py showmigrations
```

Должны быть показаны все миграции с пометкой `[X]` (выполнены).

---

## 📝 Что делать дальше

После того, как файлы загружены и зависимости установлены:

1. **Импортировать данные** (если есть бэкап) - см. `BACKUP_AND_RESTORE.md`
2. **Настроить веб-приложение** - см. `QUICK_MIGRATION.md`
3. **Настроить домен** - см. `DOMAIN_MIGRATION.md`

---

## 🆘 Решение проблем

### Проблема: "git: command not found"

**Решение:** Git должен быть доступен по умолчанию. Если нет:
```bash
# Проверь, установлен ли git
which git

# Если нет, попробуй установить через bash (не всегда работает)
# Или используй Способ 2 (загрузка вручную)
```

### Проблема: "Permission denied" при клонировании

**Решение:** Убедись, что находишься в домашней директории:
```bash
cd ~
pwd  # Должно показать: /home/имя_пользователя
```

### Проблема: "No such file or directory"

**Решение:** Проверь, что файлы действительно скопировались:
```bash
cd ~
ls -la playandjump
```

Если папки нет, значит клонирование не удалось. Попробуй еще раз или используй Способ 2.

### Проблема: "pip: command not found"

**Решение:** Используй `pip3` вместо `pip`:
```bash
pip3 install --upgrade pip
pip3 install -r requirements_pythonanywhere.txt
```

---

## ✅ Чек-лист после загрузки:

- [ ] Проект склонирован из Git (или загружен вручную)
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] Миграции выполнены
- [ ] Суперпользователь создан
- [ ] Файлы на месте (проверено через `ls`)

---

## 🎯 Быстрая команда (все в одном)

Если хочешь сделать все сразу, выполни в консоли Bash:

```bash
cd ~ && \
git clone https://github.com/MitrofanovDm89/Site.git playandjump && \
cd playandjump && \
python3.10 -m venv venv && \
source venv/bin/activate && \
pip install --upgrade pip && \
pip install -r requirements_pythonanywhere.txt && \
python manage.py migrate && \
echo "✅ Готово! Теперь создай суперпользователя: python manage.py createsuperuser"
```

Эта команда:
1. Клонирует проект
2. Создает виртуальное окружение
3. Устанавливает зависимости
4. Выполняет миграции
5. Готово к созданию админа

